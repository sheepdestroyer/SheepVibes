Title: "🧹 [code health improvement] Refactor _process_folder_node signature"

Description:
* 🎯 **What:** The `_process_folder_node` function in `backend/feed_service.py` had too many parameters (6), leading to a code health issue. This has been refactored to accept the `outline_element` directly.
* 💡 **Why:** Passing `outline_element` directly simplifies the function signature and reduces the overhead of parsing and passing the same data across multiple function calls. It centralizes the data access inside the processing function itself, which improves readability and code maintainability.
* ✅ **Verification:** The refactoring has been verified by running the comprehensive unit tests suite (`pytest tests/`), resulting in all 148 tests passing successfully with no new errors introduced. Formatting has also been passed (`flake8`, `autopep8`).
* ✨ **Result:** A more readable, maintainable, and cleaner code structure in the backend OPML import feature without changing existing functionality.
